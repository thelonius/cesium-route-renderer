# Docker Mode Configuration
# Set this environment variable to run in Docker mode with a pre-configured route
# VITE_GPX_ROUTE=alps-trail.gpx

# Leave empty or unset for Web mode (user selects route)
